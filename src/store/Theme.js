import { createTheme } from "@material-ui/core";

const theme = createTheme({
    palette: {
        primary: {
            main: "#FF6D00",
            white: "#ffffff"
        },
        secondary: {
            main: "#A9CCFF"
        }
    }
})

export default theme;